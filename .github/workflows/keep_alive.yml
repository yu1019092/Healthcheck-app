name: Keep Streamlit App Alive

on:
  schedule:
    # 日本時間 毎朝5時（UTC 20:00）に実行
    - cron: '0 20 * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Access Streamlit App
        run: curl -s https://healthcheck-app-yu1019092a.streamlit.app/
